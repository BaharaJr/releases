name: 'build-test'
on:
  pull_request:
    branches: [release]

jobs:
  test: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: BaharaJr/merge-pr@0.0.2
        with:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
